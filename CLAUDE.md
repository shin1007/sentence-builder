# CLAUDE.md

## 作業の進め方

- 実装を頼まれたら、**言われなくても PR 作成からマージまで進める**。いちいち確認しなくてよい。
  1. 作業ブランチでコミットしてプッシュする
  2. PR を作る
  3. CI（lint・テスト・ビルド）が通るのを確認する。落ちたら原因を直して再プッシュする
  4. レビューコメントがあれば対応する
  5. CI が緑でコンフリクトがなければ **squash マージ**する（PR タイトルに `(#番号)` が付く、これまでの履歴の形式に合わせる）
- マージ前に手元で `npm run lint` / `npm test` / `npm run build` を通しておく。
- 仕様上の判断が分かれるもの（ゲームバランスや UX を大きく変えるもの）は、実装前に相談する。マージの可否は相談しなくてよい。

## プロジェクトのメモ

- 設計上の論点と対応履歴は `docs/GAME_DESIGN_CONSIDERATIONS.md` にまとめている。変更したらここも更新する。
